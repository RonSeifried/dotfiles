# Core dependencies — official repos, installed via: pacman -S --needed
# Derived from what the repo actually invokes (niri spawns, Quickshell Process
# calls, scripts, .zshrc) — NOT a hand-copied list. One package per line.

# Compositor + shell
niri
quickshell          # official repo — do NOT use quickshell-git (source build)
stow                # used by install.sh for linking

# Wayland session
kanshi
swayidle
swaybg
wl-clipboard        # wl-copy / wl-paste
wl-clip-persist
cliphist
wireplumber         # wpctl (audio control)
gnome-keyring
networkmanager      # nmcli
bluez
bluez-utils

# Hardware / media control
brightnessctl
playerctl
grim
slurp
imagemagick         # magick (wallpaper thumbnails)

# Theming / utilities
# (wallust is AUR-only — see aur.txt)
jq
gum
awww                # wallpaper daemon (official repo, NOT awww-bin)
plocate             # locate

# Terminal + shell environment
kitty
zsh
tmux
neovim
yazi
starship
fzf
eza
zoxide
bat
fastfetch
